= render "main-video"
//= render "main"

%section.colored.primary#about
  =render "about"

%section.funds#funds
  = render "funds"
  
%section.faqs#faqs
  %h1.title FREQUENTLY ASKED QUESTIONS
  .container
    .half-block.faqs-left
      = render "faqs-left"
    .half-block.faqs-right
      = render "faqs-right"
%section.newsletters#newsletters
  .container
    = render "news"
%section.board#board
  .container
    = render "board"
%section.contact#contact
  .container
    = render "contact"
%section.careers#careers
  .container
    = render "careers"
